# Solution

we are given:

- an array
- a target

add verification to be sure array isn't empty otherwise return null

we should return the 2 indices as
i1 + i2 = target
so we can consider that i1 = target - i2

we can use a hash map pattern

